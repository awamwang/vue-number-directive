import Vue from 'vue/dist/vue.common.dev'
import VueNumber from '../../../dist/index'
Vue.use(VueNumber)

beforeEach(function () {
  // cy.fixture('options').as
})

describe('基本检查', function () {
  it('xxx', () => {})
})
